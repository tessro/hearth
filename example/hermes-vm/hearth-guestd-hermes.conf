[Service]
# This workload image has an authenticated Hermes install, not authenticated
# Codex/Claude installs. Advertise only the adapter the guest can actually run.
Environment=HEARTH_GUESTD_NO_CODEX=true
Environment=HEARTH_GUESTD_HERMES=/home/agent/.local/bin/hermes
